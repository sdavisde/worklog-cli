# Azure DevOps Configuration
# Replace with your actual organization and project names
ADO_ORG=your-organization-name
ADO_PROJECT=your-project-name

# Editor preference (optional, defaults to nvim)
EDITOR=nvim 